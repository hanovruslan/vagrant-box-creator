# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
    config.vm.define "my-debian" do |machine|
        machine.vm.box = "my-debian"
        machine.vm.hostname = "my-debian"
        machine.vm.provider :virtualbox do |provider|
            provider.name = "my-debian"
        end
        machine.vm.network :private_network, type: "dhcp"
    end
end
