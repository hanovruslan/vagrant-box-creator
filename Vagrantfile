# -*- mode: ruby -*-
# vi: set ft=ruby :
$machine = ENV["machine"]
$file = ENV["file"]
$path = ENV["path"]
$box = ENV["box"]
load File.expand_path($file, $path)
load File.expand_path("vagrant/post.Vagrantfile", File.dirname(__FILE__))
